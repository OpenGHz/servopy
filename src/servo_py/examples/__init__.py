"""Bundled demos and model assets; run with ``python -m servo_py.examples.<name>``.

The build installs these modules from the repository's ``examples/`` directory.
MuJoCo is optional and is only imported when its demo is used.
"""
